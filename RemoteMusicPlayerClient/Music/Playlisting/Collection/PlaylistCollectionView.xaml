<UserControl x:Class="RemoteMusicPlayerClient.Music.Playlisting.Collection.PlaylistCollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RemoteMusicPlayerClient.Music.Playlisting.Collection"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Vertical">
        <ItemsControl Items="{Binding All}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <CheckBox>
                        <CheckBox.Style>
                            <Style>
                                <Style.Triggers>
                                    <DataTrigger Value="{Binding}"></DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </CheckBox.Style>
                    </CheckBox>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
